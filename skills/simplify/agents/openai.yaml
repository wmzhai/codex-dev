interface:
  display_name: "simplify"
  short_description: "在保持行为不变前提下精简代码 diff，常用于 checkpoint 前收窄 patch"
  default_prompt: "使用 $simplify 在保持行为、API 和依赖不变的前提下精简给定代码 diff。"

policy:
  allow_implicit_invocation: false
