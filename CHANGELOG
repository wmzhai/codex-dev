# Changelog

## 0.1.2 - 2026-04-06

- 统一 quickship / checkpoint 及相关仓库文档中的版本规则表述，收敛为当前 `X.Y.Z` 约定。
- 精简版本工件与收尾说明，移除过时迁移细节，并同步规则测试与记忆文档。

## 0.1.1 - 2026-04-05

- 调整 `codev-quickship` 支持无 task 收尾模式，并要求在 `CHANGELOG` 记录本轮改动摘要。
- 调整 `codev-checkpoint` 默认不再自动同步 `VERSION` / `CHANGELOG`，并同步相关说明与规则测试。

## 2026-03-31

- 重组工作流文档入口，并同步记忆体系与相关说明文档。
- 去除 `setup` 对 `~/gstack` 安装链路的依赖，并同步 smoke test 与协作文档边界。
- 调整 `setup` 默认同时安装 Claude 与 Codex，并同步安装说明与 smoke test。

## 2026-03-27

- 同步 `codev-quickship` 说明文档，使其明确默认以当前 git 分支为起点并推送到默认主干。
- 统一 quickship 收尾提交信息格式为 `type: 具体工作摘要 (vX.Y.Z)`，并强调版本号放在末尾括号中。
- 完成 quickship 版本化收尾，递增 `VERSION` 并同步相关规则与说明。
- 初始化 `VERSION` 和 `CHANGELOG`，为后续收口提交提供可递增的版本工件。
