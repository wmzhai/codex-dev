# Changelog

## Unreleased

- 面向用户提示下一步 skill 入口时，按宿主写调用前缀：Codex 用 `$name`，Grok 用 `/name`，避免 Grok 对话里再出现 `$codev-taskdev` 这类提示。

## 0.1.5 - 2026-08-13

- 受管 skill 的工作区改为从当前目录、当前 git 根和仓库本地规则解析，不再写死 `optdev` 或其他项目路径。
- 将 `codev-issue2task` 的 `optworks#70` 示例改为通用 `<subdir>#70`。
- 调整 `setup` 默认同时刷新 Codex 与 Grok 全局 skills，并同步安装说明与 smoke test。
- 精简 `codev-quickship` / `codev-checkpoint` 的收口职责：用户触发即表示 taskdev 收尾校验和人工验证已完成，收口阶段不再运行 build/test/lint/typecheck 或脚本验证。
- 明确 `codev-checkpoint` 仍需在本阶段形成并推送收口提交（与版本/Tag 无关）。
- 细化 `codev-quickship` 执行顺序：quickship 的第一阶段不提交，改为版本 bump 后统一最终提交（供用户确认）。

## 0.1.4 - 2026-06-10

- 收紧多仓 `codev-quickship` 的版本归属规则，禁止把一个仓库的版本号、提交后缀或 tag 套用到未实际 bump 的仓库。
- 调整 `codev-quickship` 通用收尾规则：版本同步后按仓库本地规则物化并校验版本工件，提交前必须重新检查工作区，且不在通用 skill 中写死生态专属命令。
- 收紧无 task quickship 的最小校验语义，避免默认重复完整 build。
- 调整 `codev-checkpoint`：若存在对应任务文件，默认补齐任务记录并归档到 `tasks/done/`，同时保留未发布 `CHANGELOG` 聚合摘要。
- 细化 `codev-quickship` 执行模型：第一阶段完成后自动进入版本/Tag 阶段，去除每次执行的额外确认交互。

## 0.1.3 - 2026-05-30

- 调整 `codev-quickship` 版本规则，优先读取仓库本地规则；没有本地规则时兼容三段和四段数字版本，并递增最后一段。
- 调整 `codev-quickship` 收尾流程，要求同步 `CHANGELOG`、创建并推送 `v<VERSION>` tag。
- 调整 `codev-checkpoint` 默认行为，记录已有 `CHANGELOG` 的未发布内容，但不升级 `VERSION`、不创建或推送 tag。
- 同步 skill 文档、agent 默认提示、记忆规则和版本规则测试。

## 0.1.2 - 2026-04-06

- 统一 quickship / checkpoint 及相关仓库文档中的版本规则表述，收敛为当前 `X.Y.Z` 约定。
- 精简版本工件与收尾说明，移除过时迁移细节，并同步规则测试与记忆文档。

## 0.1.1 - 2026-04-05

- 调整 `codev-quickship` 支持无 task 收尾模式，并要求在 `CHANGELOG` 记录本轮改动摘要。
- 调整 `codev-checkpoint` 默认不再自动同步 `VERSION` / `CHANGELOG`，并同步相关说明与规则测试。

## 2026-03-31

- 重组工作流文档入口，并同步记忆体系与相关说明文档。
- 去除 `setup` 对 `~/gstack` 安装链路的依赖，并同步 smoke test 与协作文档边界。
- 调整 `setup` 默认同时安装 Claude 与 Codex，并同步安装说明与 smoke test。

## 2026-03-27

- 同步 `codev-quickship` 说明文档，使其明确默认以当前 git 分支为起点并推送到默认主干。
- 统一 quickship 收尾提交信息格式为 `type: 具体工作摘要 (vX.Y.Z)`，并强调版本号放在末尾括号中。
- 完成 quickship 版本化收尾，递增 `VERSION` 并同步相关规则与说明。
- 初始化 `VERSION` 和 `CHANGELOG`，为后续收口提交提供可递增的版本工件。
